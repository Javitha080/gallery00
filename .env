# This file is used for local development only
# Railway will automatically provide these variables in production

# Database connection string (will be provided by Railway in production)
DATABASE_URL=postgresql://neondb_owner:npg_RfTdOSA9NrU5@ep-damp-moon-a18u277x-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Server port (Railway will set PORT automatically)
PORT=8080

# Session secret for authentication (use a strong random value in production)
SESSION_SECRET=your-strong-session-secret-here

# Admin user credentials (set these in Railway variables for production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=securepassword

# Node environment
NODE_ENV=development